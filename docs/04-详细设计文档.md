# 详细设计文档

> 本文档统一记录所有工作项的详细设计，与 02-计划及当前执行情况.md 中的工作项严格映射。
> **维护规则**：严禁全覆盖写入。仅允许追加新章节或修改特定段落。

---

## MVP-01-T01: 通用 Docker 配置抽取

### 1. 目录结构
```text
docker-configs/
├── docker-compose.base.yml  # 基础中间件配置
├── openmoss-config/         # OpenMOSS 配置
│   └── config.yaml
└── projects/
    └── multi-agent-flow/
        └── docker-compose.yml # 项目特定服务
```

### 2. 核心配置 (docker-compose.base.yml)
*   **OpenClaw Gateway**: 使用命名卷 `openclaw-config` 避免权限问题。
*   **OpenMOSS**: 官方镜像 `ghcr.io/uluckyxh/openmoss:latest`，挂载配置和数据卷。
*   **PostgreSQL/Redis**: 命名卷持久化。

---

## MVP-01-T02: OpenMOSS API 集成验证

### 1. 验证结论
*   **角色绑定**: `assigned_agent` 字段有效，任务自动进入 `assigned` 状态。
*   **执行方法**: `description` 字段承载 `execution_context` 指令。
*   **状态流转**: `assigned` -> `in_progress` -> `review` 通畅。

---

## MVP-01-T03: 网络与端口规划

### 1. 网络拓扑
*   **Network**: `maf-net` (Bridge)。
*   **连通性**: Backend, OpenMOSS, OpenClaw, DB, Redis 均在此网络。

### 2. 端口映射
| 服务 | 容器端口 | 宿主机端口 |
|------|---------|-----------|
| OpenClaw | 18789 | 18789 |
| OpenMOSS | 6565 | 6565 |
| Backend | 8000 | 8000 |
| PostgreSQL | 5432 | 5432 |
| Redis | 6379 | 6379 |

---

## MVP-02-T01: FastAPI 骨架搭建

### 1. 项目结构
```text
code/backend/
├── app/
│   ├── main.py          # 入口，CORS，路由注册
│   ├── config.py        # Pydantic Settings
│   ├── core/
│   │   └── database.py  # SQLAlchemy Async Engine
│   └── api/
│       └── health.py    # 健康检查接口
├── Dockerfile
└── requirements.txt
```

### 2. 关键实现
*   **Database**: `postgresql+asyncpg://...`
*   **Health Check**: `GET /api/v1/health` 返回 DB 连接状态。

---

## MVP-02-T02: 模板管理与核心架构

### 1. 核心依赖项

*   **外部服务**:
    *   **Redis**: 存储模板定义、运行中 DAG 状态、配置缓存。
    *   **PostgreSQL**: 存储任务实例快照、执行日志、审计记录。
*   **Python 库**:
    *   `pyyaml`: YAML 解析。
    *   `pydantic`: 数据校验与 Schema 定义。
    *   `networkx`: DAG 依赖检查与拓扑排序。
    *   `watchdog`: 监控 `templates/` 目录变化。
    *   `httpx`: 异步调用 OpenMOSS API。
    *   `jinja2`: Prompt 模板渲染 (Input Mapping)。

### 2. 模板加载与校验逻辑

**上下文**：`template/` 目录下的 YAML 文件通过 Watchdog 监控，变动时自动加载。

**实现逻辑**：
1.  **文件读取**：读取 YAML 内容。
2.  **Schema 校验**：使用 Pydantic `TemplateSchema` 校验结构。
3.  **DAG 校验**：
    *   构建有向图。
    *   检查是否存在循环依赖 (Cycle Detection)。
    *   检查 `dependencies` 引用的 `task_id` 是否存在。
4.  **缓存更新**：校验通过后，序列化存入 Redis `template:{id}`。

### 3. 单元测试记录

| # | 测试用例 | 测试内容 | 结果 |
|---|---------|---------|------|
| 1 | `test_valid_template` | 有效模板定义校验 | ✅ PASSED |
| 2 | `test_dag_no_cycle` | DAG无循环依赖 | ✅ PASSED |
| 3 | `test_dag_with_cycle_should_fail` | DAG循环依赖应失败 | ✅ PASSED |
| 4 | `test_missing_dependency_should_fail` | 依赖不存在应失败 | ✅ PASSED |
| 5 | `test_task_types` | 不同任务类型(fixed/dynamic/review) | ✅ PASSED |
| 6 | `test_execution_context` | 执行上下文结构 | ✅ PASSED |
| 7 | `test_output_definition` | 输出定义结构 | ✅ PASSED |
| 8 | `test_control_settings` | 执行控制设置 | ✅ PASSED |
| 9 | `test_load_yaml_file` | YAML文件加载 | ✅ PASSED |
| 10 | `test_invalid_yaml_should_fail` | 无效YAML应失败 | ✅ PASSED |
| 11 | `test_empty_yaml_should_fail` | 空YAML文件应失败 | ✅ PASSED |
| 12 | `test_parallel_tasks` | 并行任务定义 | ✅ PASSED |

**测试结果**：12/12 通过，通过率 100%

---

## MVP-02-T03: 任务管理模块

### 1. 模块定位与架构

**模块定位**：任务管理模块是 Backend 的**核心中枢**，负责将用户的业务需求转化为可执行的任务流，并驱动 OpenMOSS 和 OpenClaw 协同工作。

### 2. 核心依赖项

| 类别 | 依赖项 | 用途 |
|:---|:---|:---|
| **外部服务** | OpenMOSS (Port 6565) | 任务状态机、子任务管理、Agent 注册 |
| | OpenClaw (Port 18789) | Agent 运行时、消息注入、会话管理 |
| | PostgreSQL | 任务实例、子任务记录、执行日志持久化 |
| | Redis | 任务队列缓存、Token 缓存、分布式锁 |
| **Python 库** | `httpx` | 异步 HTTP 客户端 |
| | `networkx` | DAG 依赖管理、拓扑排序 |
| | `tenacity` | 重试机制（指数退避） |
| | `pydantic` | 数据模型校验 |

### 3. 推拉结合派发机制

```python
class Dispatcher:
    async def dispatch_task(self, sub_task: SubTaskRecord):
        """派发任务：主推 + 备拉双保险"""
        try:
            # 1. 主推模式：实时调用 OpenClaw API
            await self.push_to_openclaw(sub_task)
            sub_task.dispatch_status = "pushed"
        except Exception as e:
            logger.warning(f"OpenClaw API 失败，降级为 Pull 模式: {e}")
            # 2. 备拉模式：仅更新 OpenMOSS 状态，等待 Agent 轮询
            await self.pull_fallback(sub_task)
            sub_task.dispatch_status = "pending_pull"
```

### 4. 单元测试记录

| 测试模块 | 用例数 | 通过数 | 通过率 |
|:---|:---:|:---:|:---:|
| TokenManager | 6 | 6 | 100% |
| DAGEngine | 5 | 5 | 100% |
| Dispatcher | 2 | 2 | 100% |
| ErrorHandler | 2 | 2 | 100% |
| **总计** | **15** | **15** | **100%** |

---

## MVP-02-T04: OpenMOSSClient 封装

### 1. 模块定位
`OpenMOSSClient` 是 Backend 与 OpenMOSS 交互的唯一桥梁。

### 2. 核心设计
*   **基础通信**：使用 `httpx.AsyncClient` 进行异步 HTTP 请求。
*   **认证管理**：集成 `TokenManager`，根据操作角色自动注入 `X-Agent-Key`。
*   **重试机制**：引入 `tenacity` 库，对网络错误进行指数退避重试（最多 3 次）。

### 3. 接口定义
| 方法 | 对应 API | 角色 | 说明 |
|:---|:---|:---|:---|
| `create_sub_task` | `POST /api/sub-tasks` | Planner | 创建子任务 |
| `get_sub_task` | `GET /api/sub-tasks/{id}` | Planner | 获取详情 |
| `list_sub_tasks` | `GET /api/sub-tasks` | Planner | 列表查询 |
| `submit_sub_task` | `POST .../submit` | Executor | 提交成果 |
| `block_sub_task` | `POST .../block` | Patrol | 标记阻塞 |

### 4. 测试验证
*   **结果**：7/7 用例全部通过。

---

## MVP-02-T05: OpenClawClient 封装

### 1. 业务场景与设计逻辑
*   **业务场景**：Backend 需要获取 Agent 的**详细执行日志**和**会话历史**。
*   **🚫 架构红线**：**严禁**通过此客户端向 Agent 发送任务指令。任务下发必须且只能通过 `OpenMOSSClient` 进行。
*   **定位**：**只读审计客户端 (Read-Only Auditing Client)**。

### 2. 核心接口
*   `get_conversation(conversation_id)`: 获取指定会话的完整对话历史。
*   `list_conversations(limit, offset)`: 获取会话列表。

### 3. 测试验证
*   **结果**：4/4 用例全部通过。

---

## MVP-02-T06: 任务派发引擎

> **关联任务**: `MVP-02-T06`
> **关联需求**: `REQ-20260421-002, REQ-20260421-004`
> **状态**: 已完成（P0-1~3 修复）

### 1. 模块定位

任务派发引擎负责将任务派发给 Agent，并实现"主推 + 备拉"双保险机制。

### 2. 核心设计

#### 2.1 主推模式（Push）

通过 OpenClaw API 实时注入指令：

```python
async def push_to_openclaw(self, conversation_id: str, instruction: str):
    """主推：调用 OpenClaw 消息注入 API"""
    async with httpx.AsyncClient() as client:
        response = await client.post(
            f"{self.openclaw_base_url}/api/v1/message",
            headers=token_manager.get_headers("gateway", "gateway"),
            json={
                "channel": "api",
                "message": instruction,
                "conversation_id": conversation_id,
                "wait_for_response": False
            },
            timeout=10.0
        )
        response.raise_for_status()
```

#### 2.2 备拉模式（Pull）

OpenMOSS 创建子任务时已设置 `assigned_agent`，Agent 的 Cron 脚本会定期轮询 `GET /api/sub-tasks/mine` 发现新任务。备拉模式仅记录日志用于追踪。

### 3. P0-1~3 修复记录

#### 3.1 P0-1: SyncEngine 启动优化

**问题**：`main.py` 使用 `asyncio.create_task()` 在 FastAPI 启动事件中可能失效。

**修复方案**：改用 `lifespan` 上下文管理器。

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # 启动阶段
    await init_db()
    redis_client.connect()
    sync_task = asyncio.create_task(sync_engine.start_sync_loop())
    yield
    # 关闭阶段
    sync_task.cancel()
    redis_client.disconnect()

app = FastAPI(lifespan=lifespan)
```

#### 3.2 P0-2: Dispatcher 备拉模式完善

**问题**：`_pull_fallback` 仅记录日志，未明确说明备拉模式工作机制。

**修复方案**：完善逻辑说明，明确 OpenMOSS 创建子任务时已通过 `assigned_agent` 参数分配角色。

#### 3.3 P0-3: 集成测试 Mock 外部服务

**问题**：集成测试依赖真实 OpenMOSS/OpenClaw 服务。

**修复方案**：引入 `respx` 库 Mock HTTP 响应。

**测试覆盖**：8/8 测试用例全部通过。

---

## MVP-02-T07: Decomposer 动态任务分解模块

> **关联任务**: `MVP-02-T07`
> **关联需求**: `REQ-20260421-001`
> **状态**: 待开发
> **架构评审**: ARCH-VERIFY-20260424-001（9.5/10 通过）

### 1. 业务场景与设计逻辑

**业务场景**：用户提交复杂任务（如"开发用户管理系统"），系统需自动分解为多个子任务（数据库设计、API 开发、前端开发等），并派发给不同 Agent 执行。

**设计逻辑**：
1. Backend 接收任务描述，调用 LLM 进行任务分解
2. LLM 返回结构化 JSON（子任务列表、依赖关系、角色分配）
3. Backend 校验 JSON 格式，容错重试（最多 3 次）
4. 批量创建子任务到 OpenMOSS，建立 DAG 依赖关系

**架构组验证结论**（ARCH-VERIFY-20260424-001）：
- ✅ OpenMOSS API 完全支持所需字段（`assigned_agent`、`description`、`acceptance`）
- ✅ 字段映射路径清晰（直接映射 + 嵌入映射）
- ✅ DAG 循环依赖检测已有实现（MVP-02-T02 已验证）
- ✅ JSON 容错解析机制设计合理

---

### 2. YAML 动态任务节点设计

#### 2.1 动态任务节点完整结构

```yaml
tasks:
  - task_id: "dev-breakdown"
    name: "开发任务分解"
    type: dynamic                      # 🔑 动态任务类型
    
    dependencies: ["req-analysis"]     # 依赖需求分析完成
    
    target_role: "tech-lead"           # 技术负责人角色
    required_capabilities: ["task_decomposition", "architecture_design"]
    required_skills: ["decomposer-skill", "json-validator"]
    
    execution_context:
      instruction: |
        请根据需求文档（${req-analysis.output}），将开发任务分解为具体的子任务。
        
        要求：
        1. 每个子任务必须是原子性的（可在 2-4 小时内完成）
        2. 明确子任务之间的依赖关系
        3. 为每个子任务分配角色（backend/frontend/database）
        4. 输出合法的 JSON 数组
      
      input_mapping:
        requirement_doc: "${req-analysis.output}"
        tech_stack: "${input.tech_stack}"
      
      output_format: "json"
    
    output_definition:
      type: "json"
      schema:
        type: array
        items:
          type: object
          properties:
            name: {type: string}
            role: {type: string, enum: ["backend", "frontend", "database", "devops"]}
            instruction: {type: string}
            dependencies: {type: array, items: {type: string}}
            acceptance_criteria: {type: array, items: {type: string}}
          required: ["name", "role", "instruction"]
    
    control:
      timeout: 600                     # 10 分钟超时
      retry_policy:
        max_retries: 3                 # JSON 解析失败最多重试 3 次
```

#### 2.2 三种任务类型对比

| 类型 | 说明 | 子任务来源 | 适用场景 |
|:---|:---|:---|:---|
| **`fixed`** | 固定任务 | 模板中预定义 | 标准流程（如需求分析、代码审查） |
| **`dynamic`** | 动态任务 | Agent 运行时分解 | 复杂开发任务（需根据需求动态调整） |
| **`review`** | 评审任务 | 模板中预定义 | 质量关卡（如代码审查、测试验收） |

---

### 3. 字段映射详解

#### 3.1 YAML → OpenMOSS 映射表

| YAML 字段 | OpenMOSS 字段 | 映射逻辑 | 源码证据 |
|:---|:---|:---|:---|
| `target_role` | `assigned_agent` | 直接映射 | `sub_tasks.py:31` |
| `execution_context.instruction` | `description` | 渲染后映射 | `sub_tasks.py:26`（Text 类型） |
| `acceptance_criteria` | `acceptance` | 数组转字符串 | `sub_tasks.py:28`（Text 类型） |
| `task_id` | `task_id` | 直接映射 | `sub_tasks.py:24` |
| `name` | `name` | 直接映射 | `sub_tasks.py:25` |

#### 3.2 嵌入映射字段

| YAML 字段 | 处理方式 | 说明 |
|:---|:---|:---|
| `required_skills` | 嵌入 `description` | 拼接为 `## Required Skills\n- skill1\n- skill2` |
| `execution_context.input_mapping` | Backend 解析 | 渲染为 instruction 中的实际路径 |
| `execution_context.output_format` | 嵌入 `description` | 追加到指令末尾 |

#### 3.3 Backend 侧处理字段

| YAML 字段 | 处理方式 | 说明 |
|:---|:---|:---|
| `control.timeout` | Backend SyncEngine 监控 | OpenMOSS 不支持超时，由 Backend 轮询检查 |
| `control.retry_policy` | Backend 重试 | JSON 解析失败时 Backend 重试 |
| `required_capabilities` | Backend Agent 匹配 | 预留字段，用于未来 Agent 能力匹配 |

---

### 4. 核心实现逻辑

#### 4.1 Decomposer 类设计

```python
# app/core/decomposer.py
import json
import re
import logging
import networkx as nx
from typing import List, Dict, Any

from app.clients.openmoss_client import openmoss_client
from app.core.template_loader import render_template

logger = logging.getLogger(__name__)


class Decomposer:
    """动态任务分解器"""
    
    def __init__(self):
        self.max_retries = 3
    
    async def decompose_task(
        self,
        task_def: Dict[str, Any],
        context: Dict[str, Any]
    ) -> List[Dict[str, Any]]:
        """
        执行动态任务分解
        
        Args:
            task_def: YAML 中的任务定义
            context: 运行时上下文（包含前置任务输出）
        
        Returns:
            子任务列表
        """
        # 1. 渲染指令
        instruction = render_template(
            task_def["execution_context"]["instruction"],
            context
        )
        
        # 2. 调用 LLM 分解（容错重试）
        max_retries = task_def.get("control", {}).get("retry_policy", {}).get("max_retries", 3)
        
        for attempt in range(max_retries):
            try:
                # 2.1 调用 LLM
                response = await self._call_llm(instruction)
                
                # 2.2 解析 JSON
                sub_tasks = self._parse_json(response)
                
                # 2.3 Schema 校验
                self._validate_schema(sub_tasks, task_def.get("output_definition", {}).get("schema"))
                
                # 2.4 DAG 校验
                self._validate_dag(sub_tasks)
                
                logger.info(f"Task decomposition succeeded on attempt {attempt + 1}")
                break
                
            except (JSONParseError, SchemaValidationError, DAGValidationError) as e:
                logger.warning(f"Decomposition attempt {attempt + 1} failed: {e}")
                
                if attempt == max_retries - 1:
                    raise DecompositionFailed(f"Failed after {max_retries} attempts: {e}")
                
                # 重试：将错误信息发回 LLM
                instruction = self._build_retry_instruction(e, response)
        
        # 3. 注入元数据
        sub_tasks = self._inject_metadata(sub_tasks, task_def)
        
        return sub_tasks
    
    def _parse_json(self, response: str) -> List[Dict]:
        """解析 JSON（容错处理）"""
        # 1. 提取 Markdown 代码块
        match = re.search(r'```json\s*\n(.*?)\n```', response, re.DOTALL)
        if match:
            json_str = match.group(1)
        else:
            # 2. 查找第一个 [ 和最后一个 ]
            start = response.find('[')
            end = response.rfind(']')
            if start == -1 or end == -1:
                raise JSONParseError("No JSON array found")
            json_str = response[start:end+1]
        
        # 3. 解析
        try:
            return json.loads(json_str)
        except json.JSONDecodeError as e:
            raise JSONParseError(f"Invalid JSON: {e}")
    
    def _validate_dag(self, sub_tasks: List[Dict]):
        """DAG 循环依赖检测"""
        graph = nx.DiGraph()
        
        for task in sub_tasks:
            task_id = task.get("name")
            graph.add_node(task_id)
            
            for dep in task.get("dependencies", []):
                graph.add_edge(dep, task_id)
        
        if not nx.is_directed_acyclic_graph(graph):
            cycle = nx.find_cycle(graph)
            raise DAGValidationError(f"Circular dependency detected: {cycle}")
```

#### 4.2 创建子任务到 OpenMOSS

```python
# app/core/sub_task_creator.py
class SubTaskCreator:
    """子任务创建器"""
    
    async def create_sub_tasks(
        self,
        parent_task_id: str,
        sub_tasks: List[Dict[str, Any]]
    ) -> List[Dict[str, Any]]:
        """批量创建子任务到 OpenMOSS"""
        created_tasks = []
        
        for sub_task in sub_tasks:
            # 1. 构建 description（拼接 Skills + Instruction）
            description = self._build_description(sub_task)
            
            # 2. 构建 acceptance
            acceptance = "\n".join(sub_task.get("acceptance_criteria", []))
            
            # 3. 调用 OpenMOSS API
            response = await openmoss_client.create_sub_task(
                task_id=parent_task_id,
                name=sub_task["name"],
                assigned_agent=sub_task.get("role", "executor"),
                description=description,
                acceptance=acceptance,
                priority="medium",
                type="once"
            )
            
            created_tasks.append({
                "local_id": sub_task.get("id"),
                "openmoss_id": response.get("id"),
                "name": sub_task["name"],
                "dependencies": sub_task.get("dependencies", [])
            })
        
        return created_tasks
    
    def _build_description(self, sub_task: Dict) -> str:
        """构建 description（包含 Skills + Instruction）"""
        parts = []
        
        # 1. Required Skills
        if "required_skills" in sub_task:
            skills_text = "\n".join([f"- {skill}" for skill in sub_task["required_skills"]])
            parts.append(f"## Required Skills\n{skills_text}\n")
        
        # 2. Instruction
        parts.append(f"## Instruction\n{sub_task['instruction']}\n")
        
        # 3. Output Format
        if "output_format" in sub_task:
            parts.append(f"## Output Format\n{sub_task['output_format']}\n")
        
        return "\n".join(parts)
```

---

### 5. 完整执行流程示例

```
用户提交任务："开发用户管理系统"
    ↓
Backend 创建任务实例
    ↓
执行 req-analysis（固定任务）
    ↓
输出：/workspace/req-analysis/prd.md
    ↓
执行 dev-breakdown（动态任务）
    ↓
Decomposer 调用 LLM
    ↓
LLM 返回 JSON：
[
  {"name": "数据库设计", "role": "database", "dependencies": []},
  {"name": "用户 API 开发", "role": "backend", "dependencies": ["数据库设计"]},
  {"name": "前端页面开发", "role": "frontend", "dependencies": ["用户 API 开发"]}
]
    ↓
Backend 校验 JSON + DAG
    ↓
批量创建子任务到 OpenMOSS
    ↓
派发无依赖的子任务（数据库设计）
    ↓
数据库设计完成 → 解锁用户 API 开发
    ↓
用户 API 开发完成 → 解锁前端页面开发
    ↓
所有子任务完成
```

---

### 6. agency-agent 基座 Skill 设计

> **架构组验证结论**（ARCH-VERIFY-20260424-001）：
> ✅ 方案可行，参考官方 Skill 结构，Agent 侧自治设计合理

#### 6.1 什么是 agency-agent 基座 Skill？

**定义**：agency-agent 是一个**所有 Agent 统一安装的基座 Skill**，负责任务元数据解析和动态 Skill 激活。

**核心职责**：
1. 解析 `description` 中的元数据（Required Skills、Instruction、Output Format）
2. 动态加载所需 Skills（从 `/skills` 目录读取 SKILL.md）
3. 将 Skill 上下文注入到 Agent System Prompt
4. 执行任务并提交结果

#### 6.2 SKILL.md 定义

```yaml
# /skills/agency-agent/SKILL.md
---
name: agency-agent
description: 基座 Skill，所有 Agent 统一安装，负责任务解析和 Skill 激活
---

# Agency Agent

你是一个任务执行 Agent，收到任务后按以下流程执行：

## 1. 解析任务元数据

从 description 中提取以下部分：

- **Required Skills**：任务所需的能力列表
- **Instruction**：具体任务指令
- **Output Format**：期望的输出格式（json/markdown/text）

## 2. 激活技能

根据解析出的 Required Skills 列表，加载对应 Skills：

1. 检查 `/skills/{skill_name}/SKILL.md` 是否存在
2. 读取 Skill 内容
3. 将 Skill 上下文注入到 System Prompt

## 3. 执行任务

在已加载的 Skill 上下文中执行 Instruction。

## 4. 提交结果

1. 将结果写入 Output Format 指定的格式
2. 生成 `manifest.json` 声明产出物
3. 调用 OpenMOSS API 提交任务
```

#### 6.3 核心实现逻辑

##### 6.3.1 description 解析器

```python
# /skills/agency-agent/parser.py
import re
from typing import Dict, List


def parse_description(description: str) -> Dict:
    """解析 description 中的元数据"""
    result = {
        'required_skills': [],
        'instruction': '',
        'output_format': 'text'
    }
    
    # 1. 提取 Required Skills
    skills_match = re.search(r'## Required Skills\n(.*?)(?=##|$)', description, re.DOTALL)
    if skills_match:
        skills_text = skills_match.group(1).strip()
        result['required_skills'] = [
            line.strip('- ').strip()
            for line in skills_text.split('\n')
            if line.strip()
        ]
    
    # 2. 提取 Instruction
    instruction_match = re.search(r'## Instruction\n(.*?)(?=##|$)', description, re.DOTALL)
    if instruction_match:
        result['instruction'] = instruction_match.group(1).strip()
    else:
        result['instruction'] = description
    
    # 3. 提取 Output Format
    format_match = re.search(r'## Output Format\n(.*?)(?=##|$)', description, re.DOTALL)
    if format_match:
        result['output_format'] = format_match.group(1).strip()
    
    return result
```

##### 6.3.2 Skill 加载器

```python
# /skills/agency-agent/loader.py
import os
from typing import List, Dict


class SkillLoader:
    """Skill 加载器"""
    
    def __init__(self, skills_dir: str = "/skills"):
        self.skills_dir = skills_dir
    
    def load_skills(self, skill_names: List[str]) -> List[Dict]:
        """加载所需 Skills"""
        loaded_skills = []
        
        for skill_name in skill_names:
            skill_path = os.path.join(self.skills_dir, skill_name, "SKILL.md")
            
            if os.path.exists(skill_path):
                with open(skill_path, 'r', encoding='utf-8') as f:
                    skill_content = f.read()
                
                loaded_skills.append({
                    'name': skill_name,
                    'content': skill_content,
                    'path': skill_path
                })
            else:
                print(f"Warning: Skill {skill_name} not found at {skill_path}")
        
        return loaded_skills
    
    def build_system_prompt(self, loaded_skills: List[Dict], instruction: str) -> str:
        """构建 System Prompt（包含 Skill 上下文）"""
        parts = []
        
        # 1. Agency Agent 基础指令
        parts.append("# Agency Agent\n")
        parts.append("你是一个任务执行 Agent，已加载以下 Skills：\n")
        
        # 2. 加载的 Skills
        for skill in loaded_skills:
            parts.append(f"\n## Active Skill: {skill['name']}\n")
            parts.append(skill['content'])
        
        # 3. 任务指令
        parts.append(f"\n## Current Task\n")
        parts.append(instruction)
        
        return "\n".join(parts)
```

#### 6.4 完整示例

##### 6.4.1 Backend 拼接的 description

```markdown
## Required Skills
- decomposer-skill
- json-validator

## Instruction
请根据需求文档（/workspace/req-analysis/prd.md）将开发任务分解为具体的子任务。

要求：
1. 每个子任务必须是原子性的
2. 明确子任务之间的依赖关系
3. 输出合法的 JSON 数组

## Output Format
json
```

##### 6.4.2 agency-agent 解析结果

```python
{
    'required_skills': ['decomposer-skill', 'json-validator'],
    'instruction': '请根据需求文档（/workspace/req-analysis/prd.md）将开发任务分解为具体的子任务...',
    'output_format': 'json'
}
```

##### 6.4.3 加载后的 System Prompt

```markdown
# Agency Agent

你是一个任务执行 Agent，已加载以下 Skills：

## Active Skill: decomposer-skill
你是一个任务分解专家，擅长将复杂任务拆分为原子子任务...

## Active Skill: json-validator
你是一个 JSON 校验专家，擅长检查 JSON 格式和 Schema...

## Current Task
请根据需求文档（/workspace/req-analysis/prd.md）将开发任务分解为具体的子任务...
```

#### 6.5 开发工作量评估

| 任务 | 预估工时 | 说明 |
|:---|:---:|:---|
| 编写 SKILL.md | 1h | 定义工作流程和指令 |
| 实现 parser.py | 2h | 正则表达式提取元数据 |
| 实现 loader.py | 2h | Skill 加载逻辑 |
| 实现 executor.py | 2h | 执行流程编排 |
| 编写单元测试 | 2h | 测试解析和加载逻辑 |
| **总计** | **9h** | 约 1.5 个工作日 |

---

### 7. 单元测试设计

| 测试用例 | 测试内容 | 预期结果 |
|:---|:---|:---|
| `test_decompose_valid_json` | 有效 JSON 解析 | 返回子任务列表 |
| `test_decompose_markdown_code_block` | Markdown 代码块提取 | 正确提取 JSON |
| `test_decompose_retry_on_invalid_json` | JSON 解析失败重试 | 最多重试 3 次 |
| `test_dag_no_cycle` | DAG 无循环依赖 | 校验通过 |
| `test_dag_with_cycle_should_fail` | DAG 循环依赖 | 抛出异常 |
| `test_build_description_with_skills` | description 拼接 | 包含 Skills + Instruction |
| `test_create_sub_tasks_in_openmoss` | 批量创建子任务 | 调用 OpenMOSS API |
| `test_parse_description_with_skills` | agency-agent 解析 | 正确提取 Skills 列表 |
| `test_load_skills_existing` | 加载已存在的 Skills | 返回 Skill 内容 |
| `test_build_system_prompt` | 构建 System Prompt | 包含 Skills + Instruction |

---

### 8. 依赖项

| 依赖 | 用途 | 状态 |
|:---|:---|:---|
| `networkx` | DAG 循环依赖检测 | ✅ 已安装（MVP-02-T02） |
| `openmoss_client` | 创建子任务 | ✅ 已实现（MVP-02-T04） |
| `template_loader.render_template` | 渲染指令变量 | ✅ 已实现（MVP-02-T02） |
| LLM 客户端 | 调用 LLM 分解任务 | ❌ 待实现 |
| agency-agent 基座 Skill | 动态 Skill 加载 | ❌ 待开发（9h） |

---

## MVP-02-T08: 评审流程模块

> **关联任务**: `MVP-02-T08`
> **关联需求**: `REQ-20260421-003`
> **状态**: 待开发

### 1. 业务场景与设计逻辑

**业务场景**：每个子任务完成后，需经过评审组（Reviewer Agent）验收，通过后方可解锁后续任务。

**设计逻辑**：
1. 子任务完成（`status=review`）后，Backend 创建评审任务
2. Reviewer Agent 根据 `acceptance_criteria` 进行验收
3. 验收通过 → 解锁后续依赖任务
4. 验收驳回 → 创建修正任务（`rework_task`），依赖原任务

---

### 2. 核心流程设计

#### 2.1 评审触发机制

```
SyncEngine 轮询 OpenMOSS 状态
    ↓
检测到 sub_task.status == "review"
    ↓
创建评审任务（type: review）
    ↓
Reviewer Agent 领取任务
    ↓
根据 acceptance_criteria 验收
    ↓
提交评审结果
    ↓
Backend 处理评审结果
    ├─ 通过 → 解锁后续依赖任务
    └─ 驳回 → 创建修正任务
```

#### 2.2 评审状态机

```
pending → assigned → in_progress → review → done（通过）
                                          ↓
                                      rework（驳回）
                                          ↓
                                    assigned（重新执行）
```

---

### 3. 核心实现逻辑

#### 3.1 ReviewEngine 类设计

```python
# app/core/review_engine.py
import logging
from typing import Dict, Any, List

from app.clients.openmoss_client import openmoss_client
from app.core.dispatcher import dispatcher

logger = logging.getLogger(__name__)


class ReviewEngine:
    """评审引擎"""
    
    async def handle_task_review(self, sub_task: Dict[str, Any]):
        """处理任务评审"""
        # 1. 获取验收标准
        acceptance_criteria = self._parse_acceptance_criteria(sub_task.get("acceptance", ""))
        
        # 2. 创建评审任务
        review_task = await self._create_review_task(sub_task, acceptance_criteria)
        
        logger.info(f"Review task created for {sub_task['id']}")
    
    async def _create_review_task(
        self,
        sub_task: Dict[str, Any],
        acceptance_criteria: List[str]
    ) -> Dict[str, Any]:
        """创建评审任务"""
        description = f"""## Review Task

请评审以下子任务的产出物：

**子任务名称**: {sub_task['name']}
**子任务 ID**: {sub_task['id']}

## Acceptance Criteria
{chr(10).join([f"- {c}" for c in acceptance_criteria])}

请逐项检查并给出评审结果（通过/驳回）。
"""
        
        response = await openmoss_client.create_sub_task(
            task_id=sub_task['task_id'],
            name=f"评审：{sub_task['name']}",
            assigned_agent="reviewer",
            description=description,
            acceptance="\n".join(acceptance_criteria),
            priority="high",
            type="once"
        )
        
        return response
    
    async def handle_review_passed(self, sub_task: Dict[str, Any]):
        """处理评审通过"""
        logger.info(f"Review passed for {sub_task['id']}")
        # 解锁后续依赖任务（由 SyncEngine 处理）
    
    async def handle_review_failed(
        self,
        sub_task: Dict[str, Any],
        comments: str
    ):
        """处理评审驳回"""
        logger.warning(f"Review failed for {sub_task['id']}: {comments}")
        
        # 创建修正任务
        rework_task = await self._create_rework_task(sub_task, comments)
        
        # 派发修正任务
        await dispatcher.dispatch_task(
            sub_task_id=rework_task['id'],
            openmoss_id=rework_task['openmoss_id'],
            role=sub_task.get('role', 'executor'),
            instruction=rework_task['instruction'],
            conversation_id=f"rework_{sub_task['id']}"
        )
    
    async def _create_rework_task(
        self,
        sub_task: Dict[str, Any],
        comments: str
    ) -> Dict[str, Any]:
        """创建修正任务"""
        instruction = f"""
        你的任务未通过评审，请根据以下意见修正：
        
        ## 评审意见
        {comments}
        
        ## 原始任务
        {sub_task.get('instruction', '')}
        
        请修正后重新提交。
        """
        
        response = await openmoss_client.create_sub_task(
            task_id=sub_task['task_id'],
            name=f"修正：{sub_task['name']}",
            assigned_agent=sub_task.get('assigned_agent', 'executor'),
            description=instruction,
            acceptance=sub_task.get('acceptance', ''),
            priority="high",
            type="once"
        )
        
        return {
            "id": f"{sub_task['id']}-rework",
            "openmoss_id": response.get("id"),
            "instruction": instruction
        }
    
    def _parse_acceptance_criteria(self, acceptance: str) -> List[str]:
        """解析验收标准（从多行文本）"""
        if not acceptance:
            return []
        
        criteria = []
        for line in acceptance.split('\n'):
            line = line.strip('- ').strip()
            if line:
                criteria.append(line)
        
        return criteria
```

---

### 4. 与 SyncEngine 集成

```python
# app/core/sync_engine.py（修改）
class SyncEngine:
    """状态同步引擎"""
    
    def __init__(self):
        self.review_engine = ReviewEngine()
    
    async def sync_sub_task(self, session, sub_task: SubTaskRecord):
        """同步单个子任务状态"""
        # ... 现有逻辑 ...
        
        # 新增：处理评审状态
        if new_status == SubTaskStatus.REVIEW:
            # 触发评审流程
            await self.review_engine.handle_task_review(om_status)
        
        elif new_status == SubTaskStatus.REWORK:
            # 处理评审驳回
            comments = om_status.get("review_comments", "")
            await self.review_engine.handle_review_failed(sub_task, comments)
```

---

### 5. 单元测试设计

| 测试用例 | 测试内容 | 预期结果 |
|:---|:---|:---|
| `test_create_review_task` | 创建评审任务 | 调用 OpenMOSS API |
| `test_handle_review_passed` | 处理评审通过 | 记录日志 |
| `test_handle_review_failed` | 处理评审驳回 | 创建修正任务 |
| `test_create_rework_task` | 创建修正任务 | 包含评审意见 |
| `test_parse_acceptance_criteria` | 解析验收标准 | 返回列表 |

---

### 6. 依赖项

| 依赖 | 用途 | 状态 |
|:---|:---|:---|
| `openmoss_client` | 创建评审/修正任务 | ✅ 已实现（MVP-02-T04） |
| `dispatcher` | 派发修正任务 | ✅ 已实现（MVP-02-T06） |
| `sync_engine` | 检测评审状态 | ✅ 已实现（MVP-02-T06） |

---

## MVP-03-T01: 三组件集成验证

> **关联任务**: `MVP-03-T01`
> **关联需求**: `REQ-20260421-002`
> **状态**: 待开发

### 1. 集成目标

验证 Backend → OpenMOSS → OpenClaw 完整调用链，确保三组件协同工作。

---

### 2. 集成步骤

#### 2.1 OpenMOSS Agent 注册

**目标**：注册 planner/executor/reviewer 角色，获取 API Key。

**步骤**：
1. 启动 OpenMOSS 服务
2. 调用 `POST /api/agents/register` 注册 Agent
3. 获取 API Key 并配置到 `.env`

**验证脚本**：
```python
# scripts/verify_openmoss_agents.py
import httpx

async def verify_agents():
    """验证 Agent 注册"""
    async with httpx.AsyncClient() as client:
        # 1. 注册 Planner
        response = await client.post(
            "http://openmoss:6565/api/agents/register",
            json={"id": "planner_001", "role": "planner"}
        )
        assert response.status_code == 200
        
        # 2. 注册 Executor
        response = await client.post(
            "http://openmoss:6565/api/agents/register",
            json={"id": "executor_001", "role": "executor"}
        )
        assert response.status_code == 200
        
        # 3. 注册 Reviewer
        response = await client.post(
            "http://openmoss:6565/api/agents/register",
            json={"id": "reviewer_001", "role": "reviewer"}
        )
        assert response.status_code == 200
        
        print("✅ All agents registered successfully")
```

#### 2.2 OpenClaw Skill 配置

**目标**：配置 multi-agent-flow Skill，绑定 Agent 对话。

**步骤**：
1. 创建 `/skills/agency-agent/SKILL.md`
2. 配置 OpenClaw 监控 `/skills` 目录
3. 验证 Skill 热重载

**验证脚本**：
```python
# scripts/verify_openclaw_skills.py
import os

def verify_skills():
    """验证 Skill 配置"""
    skills_dir = "/skills"
    
    required_skills = [
        "agency-agent",
        "decomposer-skill",
        "json-validator"
    ]
    
    for skill_name in required_skills:
        skill_path = os.path.join(skills_dir, skill_name, "SKILL.md")
        
        if os.path.exists(skill_path):
            print(f"✅ {skill_name} found")
        else:
            print(f"❌ {skill_name} missing")
```

#### 2.3 端到端联调

**目标**：验证完整调用链。

**测试场景**：
1. Backend 创建任务
2. Decomposer 分解任务
3. OpenMOSS 派发子任务
4. Agent 执行任务
5. SyncEngine 同步状态
6. 评审流程验证

---

### 3. 集成测试用例

| TC 编号 | 测试内容 | 预期结果 |
|:---|:---|:---|
| TC-01 | Backend 调用 OpenMOSS 创建子任务 | 返回子任务 ID |
| TC-02 | Agent 领取任务 | 状态变为 assigned |
| TC-03 | Agent 提交成果 | 状态变为 review |
| TC-04 | Reviewer 审查通过 | 状态变为 done |
| TC-05 | SyncEngine 同步状态 | Backend 数据库更新 |
| TC-06 | 依赖任务解锁 | 后续任务变为 ready |

---

### 4. 风险与应对

| 风险 | 影响 | 概率 | 应对措施 |
|:---|:---|:---:|:---|
| OpenMOSS 服务不可用 | 高 | 低 | 本地 Mock 开发，联调时验证 |
| Agent 执行超时 | 中 | 中 | Backend SyncEngine 超时检查 |
| Skill 加载失败 | 中 | 低 | 预装所有 Skills，启动时验证 |

---

---

## MVP-02-T07 设计方案调整记录（2026-04-24 11:30）

> **调整原因**：发现原设计错误，Decomposer 不应直接调用 LLM，应通过 OpenMOSS 启动 Agent 完成任务分解。

### 1. 原设计方案（已废弃）

```python
# ❌ 错误设计：Decomposer 直接调用 LLM
class Decomposer:
    async def decompose_task(self, task_def, context):
        # 1. 渲染指令
        instruction = self._render_template(...)
        
        # 2. 直接调用 LLM
        response = await self._call_llm(instruction)  # ← 错误！
        
        # 3. 解析 JSON
        sub_tasks = self._parse_json(response)
```

**废弃原因**：
- 违反系统架构原则：系统不直接调用 LLM
- 所有任务（包括任务分解）都应通过 OpenMOSS 启动 Agent 完成

---

### 2. 新设计方案（正确）

#### 2.1 核心设计逻辑

```python
# ✅ 正确设计：通过 OpenMOSS 启动 Agent 完成任务分解
class Decomposer:
    """动态任务分解器"""
    
    async def decompose_task(
        self,
        task_def: Dict[str, Any],
        context: Dict[str, Any],
        parent_task_id: str
    ) -> List[Dict[str, Any]]:
        """
        执行动态任务分解（通过 OpenMOSS 启动 Agent）
        
        Args:
            task_def: YAML 中的任务定义
            context: 运行时上下文（包含前置任务输出）
            parent_task_id: 父任务 ID
        
        Returns:
            子任务列表
        """
        # 1. 渲染指令
        instruction = self._render_template(
            task_def["execution_context"]["instruction"],
            context
        )
        
        # 2. 创建分解任务到 OpenMOSS
        decompose_task = await openmoss_client.create_sub_task(
            task_id=parent_task_id,
            name=task_def.get("name", "任务分解"),
            assigned_agent=task_def.get("target_role", "tech-lead"),
            description=instruction,
            priority="high"
        )
        
        # 3. 等待 Agent 完成（通过 SyncEngine 轮询）
        result = await self._wait_for_completion(decompose_task['id'])
        
        # 4. 解析 Agent 返回的 JSON
        sub_tasks = self._parse_json(result.get('output', ''))
        
        # 5. 校验 DAG
        self._validate_dag(sub_tasks)
        
        return sub_tasks
    
    async def _wait_for_completion(self, openmoss_id: str) -> Dict:
        """
        等待 Agent 完成任务
        
        实现方案：Event 驱动
        1. 创建 asyncio.Event
        2. SyncEngine 检测到状态变化后设置 Event
        3. 此处等待 Event 被设置
        """
        # TODO: 实现 Event 驱动等待机制
        pass
```

#### 2.2 完整工作流程

```
YAML 定义：
  - task_id: "dev-breakdown"
    type: dynamic
    target_role: "tech-lead"
    instruction: "请根据需求文档分解任务..."
    ↓
Backend Decomposer：
  1. 创建子任务到 OpenMOSS（assigned_agent="tech-lead"）
  2. OpenMOSS 调度 tech-lead Agent
  3. Agent 通过 OpenClaw 执行任务（加载 agency-agent + decomposer-skill）
  4. Agent 返回 JSON 结果
  5. SyncEngine 检测到状态变化（done）
  6. Backend 解析 JSON，创建后续子任务
```

#### 2.3 与 SyncEngine 集成

```python
# app/core/sync_engine.py（修改）
class SyncEngine:
    """状态同步引擎"""
    
    async def sync_sub_task(self, session, sub_task: SubTaskRecord):
        """同步单个子任务状态"""
        # ... 现有逻辑 ...
        
        # 新增：处理动态分解任务完成
        if new_status == SubTaskStatus.DONE:
            if sub_task.is_decomposition_task:
                # 1. 解析 Agent 返回的 JSON
                decomposer = Decomposer()
                sub_tasks = decomposer._parse_json(sub_task.decomposition_output)
                
                # 2. 批量创建后续子任务
                creator = SubTaskCreator()
                await creator.create_sub_tasks(sub_task.instance_id, sub_tasks)
                
                logger.info(f"Decomposition task completed, created {len(sub_tasks)} sub-tasks")
            else:
                # 普通任务，解锁后续依赖
                await self._unlock_dependent_tasks(session, sub_task)
```

#### 2.4 数据库模型调整

```python
# app/models/task.py（新增字段）
class SubTaskRecord(Base):
    """子任务记录模型"""
    __tablename__ = "sub_task_records"
    
    # ... 现有字段 ...
    
    # 新增字段
    is_decomposition_task = Column(
        Boolean, 
        default=False, 
        comment="是否为动态分解任务"
    )
    decomposition_output = Column(
        Text, 
        nullable=True, 
        comment="分解任务返回的 JSON"
    )
```

---

### 3. 对 YAML 模板的影响

#### 3.1 字段含义调整

| YAML 字段 | 原含义（错误） | 新含义（正确） |
|:---|:---|:---|
| `target_role` | LLM 角色提示 | OpenMOSS Agent 角色 ID |
| `execution_context.instruction` | 直接给 LLM 的指令 | 给 Agent 的完整指令（含 ## Required Skills） |
| `required_skills` | 无实际用途 | Agent 需加载的 Skills |

#### 3.2 示例模板

```yaml
tasks:
  - task_id: "dev-breakdown"
    name: "开发任务分解"
    type: dynamic
    
    dependencies: ["req-analysis"]
    
    target_role: "tech-lead"           # ← OpenMOSS Agent 角色
    required_skills: ["decomposer-skill"]
    
    execution_context:
      instruction: |
        ## Required Skills
        - decomposer-skill
        - json-validator
        
        ## Instruction
        请根据需求文档（${req-analysis.output}），将开发任务分解为具体的子任务。
        
        要求：
        1. 每个子任务必须是原子性的
        2. 明确子任务之间的依赖关系
        3. 输出合法的 JSON 数组
      
      input_mapping:
        requirement_doc: "${req-analysis.output}"
      
      output_format: "json"
    
    output_definition:
      type: "json"
      schema:
        type: array
        items:
          type: object
          properties:
            name: {type: string}
            role: {type: string, enum: ["backend", "frontend", "database"]}
            instruction: {type: string}
            dependencies: {type: array, items: {type: string}}
          required: ["name", "role", "instruction"]
```

---

### 4. 实施计划

| 阶段 | 任务 | 预估工时 | 说明 |
|:---|:---|:---:|:---|
| **阶段 1** | 修正 Decomposer 核心逻辑 | 4-6h | 移除 LLM 调用，改为 OpenMOSS API |
| **阶段 2** | 实现异步等待机制 | 6-8h | Event 驱动方案 |
| **阶段 3** | 修改 SyncEngine | 4-5h | 增加动态任务完成处理 |
| **阶段 4** | 数据库模型调整 | 1h | 新增字段 + 迁移脚本 |
| **阶段 5** | 重写测试 | 3-4h | Mock OpenMOSS API |
| **总计** | - | **18-24h** | 约 2-3 个工作日 |

---

---

## MVP-02-T07 阶段 3：SyncEngine 集成（2026-04-24 13:00）

> **关联任务**: `MVP-02-T07` 阶段 3
> **关联需求**: `REQ-20260421-001`
> **状态**: 已完成（架构评审 8.17/10 有条件通过，整改中）

### 1. 设计目标

在 SyncEngine 中增加动态任务完成后的特殊处理逻辑，区分动态分解任务和普通任务。

### 2. 核心设计

#### 2.1 SyncEngine 修改

**文件**：`app/core/sync_engine.py`

**新增方法**：`_handle_decomposition_complete()`

```python
async def _handle_decomposition_complete(
    self, 
    session, 
    sub_task: SubTaskRecord, 
    om_status: Dict
):
    """处理动态分解任务完成"""
    # 1. 从 OpenMOSS 状态信息中获取输出
    output = om_status.get("output", "") or om_status.get("deliverable", "")
    
    # 2. 保存到数据库
    sub_task.decomposition_output = output
    
    # 3. 通知 Decomposer 任务完成
    self.decomposer.notify_completion(
        sub_task.openmoss_id,
        {"output": output}
    )
```

**修改方法**：`sync_sub_task()`

```python
if new_status == SubTaskStatus.DONE:
    # 检查是否为动态分解任务
    if getattr(sub_task, 'is_decomposition_task', False):
        await self._handle_decomposition_complete(session, sub_task, om_status)
    else:
        # 普通任务，解锁后续依赖
        await self._unlock_dependent_tasks(session, sub_task)
```

#### 2.2 数据库模型修改

**文件**：`app/models/task.py`

**新增字段**：
```python
class SubTaskRecord(Base):
    # ... 现有字段 ...
    
    is_decomposition_task = Column(Boolean, default=False, comment="是否为动态分解任务")
    decomposition_output = Column(Text, nullable=True, comment="分解任务返回的 JSON")
```

### 3. 工作流程

```
SyncEngine 轮询 OpenMOSS 状态
    ↓
检测到 sub_task.status == "done"
    ↓
检查 is_decomposition_task 标志
    ├─ 是 → 调用 _handle_decomposition_complete()
    │        ├─ 获取 output 字段
    │        ├─ 保存到 decomposition_output
    │        └─ 调用 decomposer.notify_completion()
    │
    └─ 否 → 调用 _unlock_dependent_tasks()（原有逻辑）
```

### 4. 单元测试

| TC 编号 | 测试内容 | 预期结果 | 状态 |
|:---|:---|:---|:---:|
| TC-SYNC-01 | 正常完成通知 | decomposition_output 保存，notify_completion 调用 | ✅ PASSED |
| TC-SYNC-02 | 无输出情况（使用 deliverable） | 使用 deliverable 字段 | ✅ PASSED |
| TC-SYNC-03 | 异常处理 | 记录错误日志，调用 notify_failure | ✅ PASSED |

### 5. 架构评审记录

**评审 ID**: ARCH-PHASE3-REVIEW-20260424-001
**评审日期**: 2026-04-24 13:15
**综合评分**: 8.17/10（有条件通过）

**P1 整改清单**：
1. ✅ 设计文档未更新 → 已追加本章节
2. ✅ 计划文档状态滞后 → 已更新 02-计划及当前执行情况.md
3. ✅ 数据库迁移脚本缺失 → 已创建迁移脚本
4. ⏳ 输出获取逻辑不完整 → 待确认 OpenMOSS API 字段
5. ⏳ 缺少集成测试 → 待补充

---

*文档维护规则：每轮对话结束后，将符合详细设计范畴的内容追加到本文件。*
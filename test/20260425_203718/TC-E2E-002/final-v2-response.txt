我先查询该任务的详情，然后创建需求分析子任务。
任务已确认：**用户管理系统**（FastAPI + Vue3），模板 `simple-dev-flow`，状态 `pending`。

现在创建需求分析子任务：
拿到模板定义了，需求分析节点的 instruction 带有变量替换。我填充后重新创建：
Schema 里不需要 `output_path`，去掉后重试：
任务已在 OpenMOSS 创建（新 ID）。现在创建需求分析子任务：
子任务已创建并指派给 product-manager Agent。现在尝试启动它：
子任务已成功创建并分配。让我更新后端任务状态以保持一致性：
好的，总结一下执行情况：

---

**✅ 需求分析节点执行结果**

| 项目 | 详情 |
|---|---|
| **后端任务** | `c6251a4a-2bb8-4a7c-94a8-883f35d0b2ab` — 用户管理系统 |
| **模板** | `simple-dev-flow` |
| **OpenMOSS 任务** | `430f593d-ce1f-4fec-b3b4-4fefc135dc52` |
| **子任务 ID** | `8b31d811-0fbf-4f81-a28d-a0f47cf8e2f4` |
| **名称** | 需求分析 |
| **角色** | product-manager (planner-001) |
| **状态** | assigned（已分配，等待 Agent 执行） |
| **交付物** | PRD 需求文档（markdown 格式） |
| **输出路径** | `/home/node/.openclaw/workspace/req-analysis/prd.md` |

**⚠️ 发现问题**：后端调用 OpenMOSS 创建子任务时返回 400 错误，原因是后端在创建子任务前**没有先在 OpenMOSS 上创建父任务**。我已手动在 OpenMOSS 创建了对应的任务和子任务来绕过此问题。建议修复后端的任务同步逻辑，在首次创建子任务时自动将父任务同步到 OpenMOSS。

子任务目前已分配给 `planner-001`（product-manager Agent），等待 Agent 轮询并执行。
